# API base URL (build-time; Vite embeds this)
# Bu dosya örnek olup, gerçek değerler .env.development ve .env.production'ta tanımlıdır.
#
# npm run dev   → .env.development kullanır → http://localhost:5005/api
# npm run build → .env.production kullanır  → https://api.akilligaleri.com/api
#
# Lokal test için backend'i de çalıştırın: cd backend && npm run dev
VITE_API_BASE=http://localhost:5005/api
